//Dispositivo
//var arr=new Arraay();
//var arr=(1,2,3);
function dispositivo(){
	disp=[];
	disp['name']=device.name;
	disp['phonegap']=device.cordoba;
	disp['plataform']=device.plataform;
	disp['id']=device.uuid;
	disp['version']=device.version;
	disp['model']=device.model;
	
	return disp;
}

/*dispositivo()['name'];
var dev=dispositivo();
dev['name'];*/